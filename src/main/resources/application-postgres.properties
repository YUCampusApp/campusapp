spring.datasource.url=jdbc:postgresql://localhost:5432/campusapp_db
spring.datasource.username=postgres
spring.datasource.password=spms2025
# application.properties sets org.h2.Driver; must override for this JDBC URL
spring.datasource.driver-class-name=org.postgresql.Driver

# Override H2 dialect from application.properties when this profile is active
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
